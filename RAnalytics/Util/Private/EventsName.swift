import Foundation

/// These are the events name sent by the RAT tracker
///
/// - Note: All the events name declared in `RAnalyticsEvent.h` should be moved here in the next major version `9.0.0`.
enum EventsName {
    static let pageVisit = "pv"
    static let deeplink = "deeplink"
}
