## Agent skills

### Issue tracker

Issues live as GitHub issues in `rakutenanalytics/ios-analytics`. See `.agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles mapped to GitHub labels with the same names. See `.agents/triage-labels.md`.

### Domain docs

Single-context layout — root `CONTEXT.md` and `.agents/adr/`. See `.agents/domain.md`.
